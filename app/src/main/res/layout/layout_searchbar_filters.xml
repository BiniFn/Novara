<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:clipToPadding="false"
    android:clipChildren="false">

    <!-- Language Preset (Global) -->
    <ImageView
        android:id="@+id/filter_language_preset"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/show_language_preset_filter"
        android:padding="6dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_language" />

    <!-- Content Type (All/Manga/Novel/Video) using Swipe Pill -->
    <org.skepsun.kototoro.core.ui.widgets.SwipeFilterPillView
        android:id="@+id/filter_content_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp" />

    <!-- Source Tag / Tracker Site -->
    <ImageView
        android:id="@+id/filter_source_tag"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/filter"
        android:padding="6dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_filter_menu" />

</LinearLayout>
